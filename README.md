# VectorTrader — K线形态 Embedding 交易系统

基于 Gemini Embedding 2 的多模态形态识别 + Qdrant 向量检索交易系统。

## 原理

> "刻舟求剑" — 当前K线形态和历史上哪些形态最像？那些形态后来涨了还是跌了？

1. 用 mplfinance 生成K线图片（**保留坐标轴**，为多模态模型提供语义信息）
2. 用 Gemini Embedding 2 把图片转成 **3072维向量**
3. 存入 Qdrant 向量数据库
4. 新形态进来 → Embedding → 检索最相似的历史形态 → 统计后续涨跌 → 输出信号

## 技术栈

| 组件 | 选型 |
|------|------|
| Embedding 模型 | Gemini Embedding 2 (`gemini-embedding-2-preview`) |
| K线图生成 | mplfinance（保留 Price/Volume/时间轴 + Trend% 标题）|
| 向量数据库 | Qdrant (Docker 本地) |
| 历史数据 | Binance API (`python-binance`) |
| 语言 | Python |

## 当前参数

| 周期 | 窗口 | 步长 | 形态数 | 向量数 | K线数 |
|------|------|------|--------|--------|-------|
| 1h | 20根 | 6根 | 9065 | 9065 | 54429 |
| **4h** | **20根** | **3根** | **4512** | **4480** | **13575** |
| 1d | 20根 | 3根 | 744 | 744 | 2270 |

- 品种：BTCUSDT
- 数据范围：2020-01 ~ 2026-03
- 向量维度：3072（完整，不截断）
- Qdrant Collections：`kline_patterns`(4h)、`kline_patterns_1h`、`kline_patterns_1d`

---

## Phase 1 验证报告（2026-03-12 ~ 03-19）

### 迭代过程

| 版本 | 维度 | 坐标轴 | 样本数 | 整体准确率 | LONG | SHORT | 相似度范围 |
|------|------|--------|--------|-----------|------|-------|-----------|
| v1 | 768 | 无 | 1833 | 54.3% | 52.2% | 58.3% | 0.981~0.994 |
| v2 | 3072 | 无 | 904 | 47.8% | 55.0% | 0.0% | 0.980~0.993 |
| v3 | 3072 | 有 | 1004 | 56.4% | 65.5% | 30.0% | 0.959~0.984 |
| **v4** | **3072** | **有** | **4480** | **55.6%** | **54.8%** | **57.1%** | **0.954~0.984** |

### 关键发现

1. **坐标轴至关重要** — Gemini Embedding 2 是多模态模型，同时读图片中的视觉和文字。去掉坐标轴（axisoff=True）让模型损失了价格、时间、成交量等语义信息，相似度区分度极低（0.98~0.99）。加回坐标轴后区分度扩展到 0.95~0.98。

2. **维度影响不大** — 768维和3072维在相同条件下差异不显著。信息瓶颈在图片内容本身，不在向量维度。

3. **55.6% 的天花板** — 全量4480样本、3072维、带坐标轴的最优配置下，准确率为55.6%。比随机(50%)好，但未达到60%的实战门槛。

4. **LONG/SHORT 基本均衡** — 覆盖完整牛熊周期（2020-2026）后，LONG 54.8%、SHORT 57.1%，没有严重偏差。

5. **API 成本极低** — 4512张图片 Embedding 成本 < $1，Qdrant 免费。

### 验收结论

按原方案的判断标准：
- Hit Rate > 60% → 有价值，继续 ❌
- Hit Rate 55-60% → 需要增加过滤条件 ⚠️ **← 当前位置**
- Hit Rate < 55% → 纯形态不够，考虑其他维度 ❌

**结论：纯形态 Embedding 作为独立交易信号不够用，但作为辅助过滤条件有价值。**

---

## Phase 2 优化报告（2026-03-19）

### 优化方向 A：市场环境标签

给每个形态打 uptrend/downtrend/ranging 标签（基于前60根K线的SMA20斜率），检索时按环境过滤。

| 策略 | 样本数 | 准确率 | LONG | SHORT |
|------|--------|--------|------|-------|
| 基线（无过滤） | 1000 | 50.9% | 55.4% | 52.6% |
| 环境过滤 | 1000 | 51.6% | 56.4% | 49.5% |

**结论：环境标签作为搜索过滤条件无效（+0.7%，噪音范围）。** 但发现 uptrend 环境下基线准确率天然更高（61.7%），说明标签适合作为"何时信任信号"的输出元数据，不适合作为搜索条件。

### 优化方向 B：多周期共振 ✅

同时对 1h/4h/1d 三个周期做形态检索，只有多周期方向一致才出信号。

| 策略 | 信号数 | 准确率 | 强LONG | 强SHORT | 触发率 |
|------|--------|--------|--------|---------|--------|
| 单4h（基线） | 1000 | 50.9% | ~55% | ~52% | 100% |
| ≥2周期共识 | 813 | **55.4%** | **63.5%** | 53.8% | 81% |
| **全3周期共识** | **207** | **58.5%** | **65.9%** | 52.9% | **20.7%** |

**关键发现：**
1. **三周期全部一致时，强 LONG 信号准确率 65.9%** — 突破 60% 实战门槛
2. 触发率 20.7%，即约每 5 次检索出 1 次有意义的共振信号
3. SHORT 方向信号偏弱（52.9%），做空信号参考价值有限
4. 多周期共振是目前最有效的优化手段

### Phase 2 验收

- Hit Rate > 60%（强LONG信号）→ 有实战价值 ✅
- 信号频率合理（~20%触发率）→ 不会太稀疏 ✅
- SHORT 信号不够强 → 做空方向仅参考，不作为决策依据 ⚠️

---

## 实战运用方案

### 定位：多周期共振做多信号过滤器

Phase 2 验证后，最佳用法是 **三周期共振 + 强 LONG 信号**（65.9% 准确率）。

### 推荐用法：多周期共振确认

```
你的主信号（技术分析/BigBrother V6）
    ↓ 产生做多想法
VectorTrader 三周期检索（1h + 4h + 1d）
    ↓
如果三个周期全部看涨 → 强确认，加仓执行
如果两个周期看涨     → 弱确认，正常执行
如果方向不一致       → 谨慎或放弃
```

### 实时监控推送

```bash
# 每4小时自动检索，三周期共振后推送
python scripts/validate_multi_tf.py --samples 1 --top_k 10
```

推送到微信/Telegram，配合人工判断。做多信号优先，做空信号仅参考。

---

## 下一步优化方向

| 方向 | 难度 | 预期收益 | 状态 |
|------|------|---------|------|
| ~~市场环境标签~~ | 中 | ~~高~~ 无效 | ❌ 已验证，作为搜索过滤无效 |
| ~~多周期叠加~~ | 低 | 高 | ✅ 已完成，强LONG 65.9% |
| **实时监控推送** | 低 | 高 | 待开发 |
| **多品种扩展** | 低 | 中 | 待定 |
| **文本+图片混合Embedding** | 中 | 未知 | 待评估 |

---

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 API Key（付费层，无每日限额困扰）
echo "GEMINI_API_KEY=your_key" > .env

# 3. 启动 Qdrant
docker run -d --name qdrant -p 6333:6333 qdrant/qdrant

# 4. 一键运行全流程
python run_pipeline.py

# 或分步运行
python scripts/fetch_klines.py          # 拉数据
python scripts/generate_charts.py       # 生成图片
python scripts/embed_charts.py          # Embedding（支持断点续传）
python scripts/import_qdrant.py         # 导入Qdrant
python scripts/search_pattern.py        # 检索验证
```

## 项目结构

```
VectorTrader/
├── config/
│   ├── __init__.py
│   └── settings.py            # 全局配置
├── scripts/
│   ├── fetch_klines.py        # Step 1: 拉取 Binance 历史K线
│   ├── generate_charts.py     # Step 2: 滑动窗口生成形态图片
│   ├── embed_charts.py        # Step 3: Gemini Embedding 批量向量化
│   ├── import_qdrant.py       # Step 4: 导入 Qdrant 向量库
│   ├── search_pattern.py      # Step 5: 单周期相似形态检索
│   ├── label_regime.py        # 市场环境标签（uptrend/downtrend/ranging）
│   ├── validate.py            # 单周期批量验证（基线 vs 环境过滤）
│   └── validate_multi_tf.py   # 多周期共振验证（1h+4h+1d）
├── run_pipeline.py             # 一键运行全流程
├── data/                       # K线CSV + 向量npy（gitignore）
├── charts/                     # 形态图片（gitignore）
├── .env                        # API Key（gitignore）
└── requirements.txt
```

---

## 花费总结

| 项目 | 费用 |
|------|------|
| Gemini Embedding 2 — Phase 1 (4512张 4h) | < $1 |
| Gemini Embedding 2 — Phase 2 (9065张 1h + 744张 1d) | < $2 |
| Qdrant | 免费（本地Docker） |
| Binance 数据 | 免费 |
| 服务器 | 无（本地运行） |
| **总计** | **< $3** |
| **时间** | **Phase 1: 7天 / Phase 2: 1天** |
